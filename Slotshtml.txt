       <!-- Section 4: Availability & Slots -->
<div class="p-3 mb-4 border rounded bg-light">
  <h6 class="mb-3 text-dark fw-bold">Availability & Slots</h6>

  <div class="row mb-2" *ngFor="let availability of doctor.DoctorAvailabilityList; let i = index">
    <div class="col-md-3">
      <select class="form-control" [(ngModel)]="availability.DayOfWeek" name="DayOfWeek{{i}}" required>
        <option value="">Select Day</option>
        <option *ngFor="let day of daysOfWeek" [value]="day.value">{{ day.name }}</option>
      </select>
    </div>

    <div class="col-md-8">
      <!-- Session Slots -->
      <div *ngFor="let session of availability.Sessions; let j = index" class="border rounded p-2 mb-2 bg-white">
        <div class="row g-2 align-items-center">
          <div class="col-md-4">
            <input type="time" class="form-control" [(ngModel)]="session.StartTime"
                   name="StartTime{{i}}{{j}}" required>
          </div>

          <div class="col-md-4">
            <input type="time" class="form-control" [(ngModel)]="session.EndTime"
                   name="EndTime{{i}}{{j}}" required>
          </div>

          <div class="col-md-3">
            <input type="number" class="form-control" [(ngModel)]="session.Duration"
                   name="Duration{{i}}{{j}}" placeholder="Slot (min)" required>
          </div>

          <div class="col-md-1 text-center">
            <button type="button" class="btn btn-outline-danger btn-sm"
                    (click)="removeSession(i, j)">
              <i class="bi bi-x-lg"></i>
            </button>
          </div>
        </div>
      </div>

      <button type="button" class="btn btn-sm btn-outline-secondary mt-2"
              (click)="addSession(i)">
        + Add Session
      </button>
    </div>

    <div class="col-md-1 text-center">
      <button type="button" class="btn btn-danger btn-sm mt-1"
              (click)="removeAvailability(i)">
        X
      </button>
    </div>
  </div>

  <div class="mt-3">
    <button type="button" class="btn btn-secondary" (click)="addAvailability()">+ Add Day</button>
  </div>
</div>